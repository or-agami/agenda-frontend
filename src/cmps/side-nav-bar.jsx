import { useEffect } from "react"
import { useDispatch, useSelector } from "react-redux"
import { loadBoard } from "../store/board/board.action"

export const SideNavBar = ({board}) => {

    console.log(board);
    return <section className="side-nav-bar">
        <p></p>
    </section>
}